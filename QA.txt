# QA —— 添加字体颜色（第二轮：已参考 genshin.md，仅剩待确认问题）

已参考你提供的 `damage-formulas/src/content/genshin.md` 整理规则，见「一、已确认规则」。
以下只需确认「二、仍需确认的问题」，确认后我即开始为 13 个 Boss 文本统一应用。

---

## 一、已确认的规则（供你核对，如有误请指正）

1. **着色写法**：`<span class="元素类名">**文字**</span>`，类名 = `colors.json` 的 `genshin` 段键名。
   - 7 元素纯色类：`pyro` / `hydro` / `electro` / `cryo` / `anemo` / `geo` / `dendro`
   - 渐变反应类：`lunar-charged-electro` / `lunar-bloom-dendro` / `lunar-crystallize-geo` / `stellar-swirl-anemo` / `stellar-swirl-cryo`
   - 文字通常同时加粗（`**…**`）。

2. **数字 / 数学包裹**：
   - 行内数字用**单个** `$…$`（如 `$250\%$`、`$0.4U$`、`$90\%$`、`$1446.85$`）。
   - 整段公式用**双** `$$…$$`（独占一行）。
   - 注：你之前说"用 `$$` 包裹"，参考文件里行内实际用的是单 `$`——请在第 2 条确认 Boss 文本是否也照此用单 `$`。

3. **渐变色**：月/星反应名直接套用对应渐变类名（如 `月感电`→`lunar-charged-electro`）。

4. **着色对象（参考文件范围）**：元素词 + 元素反应名 + 月/星反应名；`物理`、`护盾` 等在参考中**只加粗不着色**。

---

## 二、仍需确认的问题

### 1. 渲染层
本项目 `src/main.js` 用 `escapeHtml()` 渲染技能文本，会把 `<span>` 转义、构建页不会显示颜色。
- [√] 需要我**同步改渲染**（`parse-md.js`/`main.js`/`style.css`，让 `<span class>` 与 `$…$` 在构建页真正渲染）？
- [ ] 还是本仓库 MD 只作**数据源/本地查看器**用途，渲染由外部处理，我只需改 MD 文本？

### 2. 行内数字分隔符
确认在 Boss 文本里，行内数字也用**单个** `$…$`（而非 `$$…$$`）？
- [√] 是，用单 `$`
- [ ] 否，用双 `$$`（请说明）

### 3. 数字包裹的边界与格式（Boss 文本特有，请确认写法）
- `45%生命值` → `$45\%$生命值`？
- `-60%`（负值）→ `$-60\%$`？
- `12U` → `$12U$`？  `36能量` → `$36$能量`？  `0.6s` → `$0.6s$`？
- `20次` / `每0.6s一次` → `$20$次` / `每$0.6s$一次`？
- 血量表行 `N5 / N6：1243,7830 / 2365,8108`：是否**不包裹**？（参考文件无此结构，我倾向不包）
A：写在md文件中时我希望可以用`每 $0.6s$ 一次`这样的方式，其他的更改方向你理解的很对
### 4. 着色词范围与映射（Boss 文本特有，参考未覆盖，请给出）
- **反应词映射确认**：
  `感电`→electro？ `超载`→pyro？ `超导`→cryo？ `冻结`→cryo？ `燃烧`→pyro？ `扩散`→anemo？ `结晶`→geo？ `绽放/超绽放/烈绽放`→dendro？ `激化/超激化/蔓激化`→?  `蒸发/融化`→?（参考中它们未着色，需确认是否着、用什么类）
- **特殊机制词**：`月感电`→`lunar-charged-electro`？ `物理`、`夜魂`、`深黯护盾`、`晦隐`、`护罩`、`元素晶球`、`元素微粒` 是否着色？用什么类名/颜色？
A：超激化→electro，蔓激化→dendro，剩下的是对的，其余的没有给出颜色的名词遇到了只需要用``强调即可
### 5. 是否需要整段 `$$…$$` 公式块
Boss 文本是描述性文字。参考文件里的 `$$…$$` 块公式在 Boss 文本里是否需要？
- [√] 不需要（只行内 `$…$`）
- [ ] 需要（请指出哪些段落）

### 6. 文件范围
确认处理 `src/content/` 下全部 13 个 `.md`（5.7 ~ 7.1）？
- [√] 是，全部
- [ ] 否（请列出需跳过的）

---

## 三、示例确认（可选但建议）
请给我 1~2 小段**Boss 原文**，标出它「着色 + 数字包裹」后的成品样子，我按此风格统一应用，避免返工。
